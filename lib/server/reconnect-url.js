module.exports = function reconnnectURL(ctx) {
    /*const leftovers = ctx.clientRequest.url.split('?')[1];
    if (leftovers && !leftovers.includes('xhr=1')) ctx.url.search = leftovers;*/
    return true;
};